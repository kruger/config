
BREW
Most important:
coreutils
fd
fzf
rust
#
applications
neomutt, notmuch, urlview, abook, lynx, gpgme, isync, khard, ripmime
visidata
tmux # Prefer over spack just to avoid the loading issues
omega, xapian, ghostscript, xpdf
dbeaver

# 1. Install Sixel which is the workhorse: brew install libsixel (https://github.com/saitoha/libsixel)
# 2. Install python bindings for it: pip install PySixel
# 3. Install matplotlib backend for pysixel: pip install https://github.com/ejolly/matplotlib-sixel

brew install miller #-- CSV CLI application

pdfjam 
brew install httpie   | https://httpie.org
brew install MisterTea/et/et  # Eternal Terminal
brew install mosh
brew install include-what-you-use
brew install colordiff
brew install ansifilter
brew install urlview
starfish
fonts (see writeup)

CONDA
conda install pyramid
conda install -c damianavila82 rise
conda install dtale -c conda-forge

See here for intel installation:
https://software.intel.com/en-us/articles/using-intel-distribution-for-python-with-anaconda


GITHUB
git clone https://github.com/hcyrnd/simon.git  
      -- MAC
      -- Python example for creating a menubar

git clone https://github.com/nvbn/thefuck.git  
      -- TOOLS
      -- Correct previous CLI command
  

git clone https://github.com/datamade/dedupe.git
      -- PYTHON/datamade
      -- python library for accurate and scaleable fuzzy matching, record deduplication and entity-resolution.

git clone https://github.com/pyamg/pyamg.git
git clone https://github.com/jedbrown/git-fat.git
git clone https://github.com/jedbrown/numerical-computation.git
git clone https://github.com/jedbrown/spectral-petsc.git
